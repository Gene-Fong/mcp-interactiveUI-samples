# Media assets

These `.gif` files are **placeholders** (1×1 transparent GIFs) referenced by
`python/README.md` §2.4 "In action". Replace each with a real screen recording
of the corresponding widget interaction, keeping the same file name:

| File | Records | Shows |
|---|---|---|
| `HS-ShowCompanies.gif` | GET | "show me companies" → company list widget |
| `HS-FilterOrders.gif` | FILTER | "orders over 5000" → filtered order list |
| `HS-CreateCompany.gif` | CREATE | pre-filled create-company form |
| `HS-EditRecord.gif` | EDIT | edit form opening with pre-filled fields |
| `HS-360Modal.gif` | DRILL | view icon → 360 modal with associated records |
| `HS-GetTasks.png` | GET | "get tasks" → task activity list widget (real screenshot) |
| `HS-FilterCompanies.png` | FILTER | "get all companies that are reseller" → filtered company list (real screenshot) |
| `HS-IdentifyCompany.png` | IDENTIFY | "show company Himalayan Encounters" → single company record (real screenshot) |
| `HS-EditContact.png` | EDIT | "edit Mariaer Johnsoner" → contact edit form (real screenshot) |
| `HS-CreateContact.png` | CREATE | "create a contact Linda with email … and company GlobalFizz" → prefilled create form (real screenshot) |
| `HS-Drill360.png` | DRILL | "get company GlobalFizz" → 360 modal with contacts, deals, tickets (real screenshot) |
| `HS-ResolveFK.png` | RESOLVE FK | "get orders for Mariaer Johnsoner" → orders filtered by resolved contact name (real screenshot) |
